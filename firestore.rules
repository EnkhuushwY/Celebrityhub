rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Хэрэглэгч зөвхөн өөрийн doc-г унших, бичих боломжтой
    match /activities/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
